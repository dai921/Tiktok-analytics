apiVersion: v1
kind: Service
metadata:
  name: account-crawler-service
  namespace: crawlers
spec:
  selector:
    app: account-crawler
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer 