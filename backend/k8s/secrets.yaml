apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
type: Opaque
stringData:
  host: "your-db-host"
  username: "your-db-user"
  password: "your-db-password"
  database: "tiktok_db"
---
# サービスアカウントキーは別途作成
# kubectl create secret generic pubsub-key --from-file=key.json=/path/to/credentials.json 